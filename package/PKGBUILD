# SPDX-FileCopyrightText: 2023 Zextras <https://www.zextras.com>
#
# SPDX-License-Identifier: AGPL-3.0-only

pkgname="carbonio-tasks-db"
pkgver="0.3.4"
pkgrel="SNAPSHOT"
pkgdesc="Carbonio Tasks database sidecar"
maintainer="Zextras <packages@zextras.com>"
section="mail"
priority="optional"
arch=('any')
copyright=(
  "2022, Zextras <https://www.zextras.com>"
)
license=(
  "AGPL-3.0-only"
)
url="https://github.com/zextras"
depends__apt=(
  "service-discover"
  "service-discover-base"
  "pending-setups"
  "carbonio-core"
  "postgresql-client"
)

depends__yum=(
  "service-discover"
  "service-discover-base"
  "pending-setups"
  "carbonio-core"
  "postgresql"
)

source=(
  "111-${pkgname}-setup.sh"
  "${pkgname}.hcl"
  "${pkgname}-bootstrap"
  "${pkgname}-setup"
  "${pkgname}-sidecar.service"
  "${pkgname}-sidecar-legacy.service"
  "intentions.json"
  "policies.json"
  "service-protocol.json"
)
backup=(
  "etc/zextras/service-discover/${pkgname}.hcl"
)

sha256sums=(
  'e19c3dae9fb03ac5604a1dd43d458fe453e62e3cb306e5e9a8cca72055dee4f1'
  '04e1cecee08385261acd5b72ecc1ad09ad6fd4be44705cff0096be9727d01749'
  'f952797fbc4b6ed0af5c05aec9595fc577d16e99f9774ead885d2e4df42c43df'
  'd426fe19ba6d14b02158688074eee6e84193d0be90de62ff845c2be1e5afeb47'
  '6d4244cee078dafd9f424a8df565794c5e21081315534552ef1894a4ca56fc87'
  'a3c630c20193775977d316ba756b6c9ef4638c450b621cad96241c5fce91e79c'
  'e31d1e76d7530734922ce14c715e62a005b3ec79a40a4cc35c8c8a5089b79571'
  '8e549e7c6c6dfe32371c03bd0d6be4f0ac82d87426a51080a6c209a91608705d'
  '81c49d3900c8fdd1fc025746759f802189c19a22f670b778d2ed91be95e30c42'
)

_package_common() {
  install -Dm644 "${srcdir}/111-${pkgname}-setup.sh" \
    "${pkgdir}/etc/zextras/pending-setups.d/111-${pkgname}-setup.sh"

  install -Dm755 "${srcdir}/${pkgname}-setup" \
    "${pkgdir}/usr/bin/${pkgname}-setup"

  install -Dm755 "${srcdir}/${pkgname}-bootstrap" \
    "${pkgdir}/usr/bin/${pkgname}-bootstrap"

  install -Dm644 "${srcdir}/${pkgname}.hcl" \
    "${pkgdir}/etc/zextras/service-discover/${pkgname}.hcl"

  install -Dm644 "${srcdir}/intentions.json" \
    "${pkgdir}/etc/carbonio/tasks-db/service-discover/intentions.json"

  install -Dm644 "${srcdir}/policies.json" \
    "${pkgdir}/etc/carbonio/tasks-db/service-discover/policies.json"

  install -Dm644 "${srcdir}/service-protocol.json" \
    "${pkgdir}/etc/carbonio/tasks-db/service-discover/service-protocol.json"
}

_package_systemd() {
  _package_common
  install -Dm644 "${srcdir}/${pkgname}-sidecar.service" \
    "${pkgdir}/lib/systemd/system/${pkgname}-sidecar.service"
}

_package_legacy() {
  _package_common
  install -Dm644 "${srcdir}/${pkgname}-sidecar-legacy.service" \
    "${pkgdir}/lib/systemd/system/${pkgname}-sidecar.service"
}

package() { _package_systemd; }
package__rocky_8() { _package_legacy; }
package__ubuntu_jammy() { _package_legacy; }

postinst() {
  getent group 'carbonio-tasks-db' >/dev/null ||
    groupadd -r 'carbonio-tasks-db'
  getent passwd 'carbonio-tasks-db' >/dev/null ||
    useradd -r -M -g 'carbonio-tasks-db' -s /sbin/nologin 'carbonio-tasks-db'

  if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || :
    systemctl enable carbonio-tasks-db-sidecar.service >/dev/null 2>&1 || :
  fi

  echo "======================================================"
  echo "Carbonio Tasks DB installed successfully!             "
  echo "You must run pending-setups to configure it correctly."
  echo "======================================================"
}

prerm() {
  if [ -d /run/systemd/system ]; then
    systemctl disable --now carbonio-tasks-db-sidecar.service >/dev/null 2>&1 || :
  fi
}

postrm() {
  if [ -d /run/systemd/system ]; then
    systemctl daemon-reload >/dev/null 2>&1 || :
  fi
}
